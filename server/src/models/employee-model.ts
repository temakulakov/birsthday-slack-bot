export default class EmployeeModel {
    constructor(public name: String, public email: String, public birthday: String) {
        this.name = name;
        this.email = email;
        this.birthday = birthday;
    }
}